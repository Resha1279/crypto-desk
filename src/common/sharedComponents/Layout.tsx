import React, { FC } from "react";

interface Props {}

const Layout: FC = (props: Props) => {
  return (
    <div>
      Layout
      <div></div>
      <footer>footer</footer>
    </div>
  );
};

export default Layout;
