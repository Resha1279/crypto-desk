import styled from "styled-components";

export const CardContainer = styled.div`
  background-color: var(--white);
  padding: 1em;

  img {
    width: 50px;
    height: 50px;
    object-fit: contain;
  }
`;
